replicas: 3

#This is just init password for vault connections.
#postgresqlPassword: "H@ckM3"
global:
  postgresql:
    password: "EQb3iRfZd0y50b"
    repmgrPassword: "H@ckEQb3iRfZd0y50b"

postgresql:
  initdbScripts:
    rbk-dbs.sql: |
       CREATE DATABASE IF NOT EXIST keycloak;
       CREATE DATABASE shumway;
       CREATE DATABASE hook;
       CREATE DATABASE fraudbusters;
       CREATE DATABASE keycloak;
  replicaCount: 3
pgpool:  
  extraEnvVars:
    - name: PGPOOL_ENABLE_POOL_HBA
      value: "no"

resources:
  requests:
    cpu: 1000m
    memory: 512Mi
