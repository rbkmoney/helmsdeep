---
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ include "keycloak-realms.name" . }}
  namespace: default
  labels:
    chart: {{ template "keycloak-realms.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "keycloak-realms.name" . }}
  policyTypes:
    - Ingress
  ingress:
    - ports:
      - protocol: TCP
        port: 8080
      from:
        - podSelector:
            matchLabels:
              service-class: capi
        - podSelector:
            matchLabels:
              service-class: wapi
