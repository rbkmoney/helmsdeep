apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxy-mocket-inspector.fullname" . }}-test-connection"
  labels:
    {{- include "proxy-mocket-inspector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxy-mocket-inspector.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never
