---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test-transaction-v2.fullname" . }}
  labels:
    {{- include "test-transaction-v2.labels" . | nindent 4 }}
data:
  sys.config: |
    {{- .Values.appConfig | nindent 4 }}
  vm.args: |
    -sname {{ include "test-transaction-v2.fullname" . }}
    -setcookie {{ include "test-transaction-v2.fullname" . }}_cookie
    -kernel inetrc '"./erl_inetrc"''
  erl_inetrc: |
    {{- .Values.vmConfig | nindent 4 }}
