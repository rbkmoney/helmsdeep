---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "capi-pcidss-v2.fullname" . }}
  labels:
    {{- include "capi-pcidss-v2.labels" . | nindent 4 }}
data:
  sys.config: |
    {{- .Values.appConfig | nindent 4 }}
  vm.args: |
    -sname {{ include "capi-pcidss-v2.fullname" . }}
    -setcookie {{ include "capi-pcidss-v2.fullname" . }}_cookie
    {{- if .Values.beam.disableSBWT }}
    +sbwt none
    {{- end }}
    # -kernel inetrc '"./erl_inetrc"''
  erl_inetrc: |
    {{- .Values.vmConfig | nindent 4 }}
