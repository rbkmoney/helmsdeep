---
apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: {{ include "cds.name" . }}
  namespace: default
  labels:
    chart: {{ template "cds.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "cds.name" . }}
  ingress:
    - toPorts:
      - ports:
        - protocol: TCP
          port: "8022"
      fromEndpoints:
        - matchLabels:
          k8s:app.kubernetes.io/name: kds
        - matchLabels:
          k8s:service-class: provider-adapter
        - matchLabels:
          k8s:service-class: capi-pcidss
        - matchLabels:
          k8s:service-class: wapi-pcidss
