apiVersion: v1
kind: Service
metadata:
  name: {{ include "capi-v1.fullname" . }}
  labels:
    {{- include "capi-v1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: api
  selector:
    {{- include "capi-v1.selectorLabels" . | nindent 4 }}
