apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payform.fullname" . }}-test-connection"
  labels:
    {{- include "payform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
