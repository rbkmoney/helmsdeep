apiVersion: v1
kind: Service
metadata:
  name: {{ include "identification.fullname" . }}
  labels:
    {{- include "identification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identification.selectorLabels" . | nindent 4 }}
