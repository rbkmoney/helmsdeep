apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identification.fullname" . }}-test-connection"
  labels:
    {{- include "identification.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identification.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
