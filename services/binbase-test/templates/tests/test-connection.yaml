apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "binbase-test.fullname" . }}-test-connection"
  labels:
    {{- include "binbase-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "binbase-test.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never
