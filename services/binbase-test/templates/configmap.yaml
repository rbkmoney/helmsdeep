---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "binbase-test.fullname" . }}
  labels:
    {{- include "binbase-test.labels" . | nindent 4 }}
data:
  entrypoint.sh: |
    {{- .Values.entrypoint | nindent 4 }}
