apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "bender.fullname" . }}-test-connection'
  labels:
    {{- include "bender.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bender.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
