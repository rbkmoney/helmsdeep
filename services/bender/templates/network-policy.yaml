---
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ include "bender.name" . }}
  namespace: default
  labels:
    chart: {{ template "bender.chart" . }}
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "bender.name" . }}
  policyTypes:
    - Ingress
  ingress:
    - ports:
      - protocol: TCP
        port: 8022
      from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: hellgate
        - podSelector:
            matchLabels:
              service-class: capi
        - podSelector:
            matchLabels:
              service-class: wapi
