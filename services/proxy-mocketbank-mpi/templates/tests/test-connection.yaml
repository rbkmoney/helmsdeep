apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxy-mocketbank-mpi.fullname" . }}-test-connection"
  labels:
    {{- include "proxy-mocketbank-mpi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxy-mocketbank-mpi.fullname" . }}:{{ .Values.apiService.port }}/actuator/health']
  restartPolicy: Never
