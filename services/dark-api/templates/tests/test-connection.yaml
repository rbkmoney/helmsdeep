apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dark-api.fullname" . }}-test-connection"
  labels:
    {{- include "dark-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dark-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
