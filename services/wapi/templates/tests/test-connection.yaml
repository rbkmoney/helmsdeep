apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wapi.fullname" . }}-test-connection"
  labels:
    {{- include "wapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wapi.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
