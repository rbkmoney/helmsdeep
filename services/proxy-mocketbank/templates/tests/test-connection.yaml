apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proxy-mocketbank.fullname" . }}-test-connection"
  labels:
    {{- include "proxy-mocketbank.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proxy-mocketbank.fullname" . }}:{{ .Values.apiService.port }}/actuator/health']
  restartPolicy: Never
