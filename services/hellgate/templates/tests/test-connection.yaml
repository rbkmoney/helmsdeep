apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellgate.fullname" . }}-test-connection"
  labels:
    {{- include "hellgate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hellgate.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
