apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "machinegun.fullname" . }}-test-connection"
  labels:
    {{- include "machinegun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "machinegun.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
