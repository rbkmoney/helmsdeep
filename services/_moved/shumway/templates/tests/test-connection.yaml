apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shumway.fullname" . }}-test-connection"
  labels:
    {{- include "shumway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shumway.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never
