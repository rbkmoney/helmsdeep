apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dominant.fullname" . }}-test-connection"
  labels:
    {{- include "dominant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dominant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
