apiVersion: v1
kind: Service
metadata:
  name: {{ include "kds.fullname" . }}
  labels:
    {{- include "kds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kds.selectorLabels" . | nindent 4 }}
