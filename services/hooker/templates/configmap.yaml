---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hooker.fullname" . }}
  labels:
    {{- include "hooker.labels" . | nindent 4 }}
data:
  entrypoint.sh: |
    {{- .Values.entrypoint | nindent 4 }}
